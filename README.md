# Minimal SPA Template

This is about as minimal as you can get for a website starter! Uses only plain HTML/CSS/JS with hash-based routing.

## Usage
```sh
python -m http.server
```
That's it!

## Modifying
To create a page, just add a new `.html` file in `pages/`.

